// .toUpperCase() te va a permitir cambiar un texto a todo mayusculas
const producto9: string = "Monitor 24 pulgadas ";
console.log(producto9.toUpperCase());

// .toLowerCase()
console.log(producto9.toLowerCase());

// .toString()
const cantidad = 200;
console.log(cantidad);
console.log(cantidad.toString());
